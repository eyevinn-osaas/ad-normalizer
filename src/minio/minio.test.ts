import { listenForNotifications } from "./minio";
import * as Minio from 'minio';


describe('minio notifications', () => {
    it('Should use supplied callback when notification is received', async () => {
        // TODO: Implement test
    });
});